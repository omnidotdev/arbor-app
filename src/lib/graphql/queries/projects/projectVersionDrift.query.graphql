query ProjectVersionDrift($projectId: UUID!) {
  projectVersionDrift(projectId: $projectId) {
    packageManager
    packageName
    versionConstraint
    repositoryId
    name
    slug
    ownerUsername
    organizationSlug
  }
}
